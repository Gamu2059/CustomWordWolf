namespace Lobby {
    /// <summary>
    /// ロビーグループの中のUIの表示単位を表す状態の列挙値。
    /// </summary>
    public enum LobbyState {
        None,
        JoinRoom,
        EditPlayerName,
        CreateRoom,
    }
}